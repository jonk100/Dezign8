---
title: Prop Report
description: Automatically generated report analyzing design system prop inheritance, overrides, and sharing.
category: Architecture
status: stable
updated: 2026-06-29
---

Generated by `plugins/props.ts`

---

## Summary

- **67** components analyzed
- **168** unique props discovered
- **4** shared base interfaces
- **12** category interfaces
- **67** component-specific interfaces

---

## Universal Props

These props are implemented by every component.

### AriaProps
`src/design/shared/aria.props.ts`

67/67 components

```
aria-atomic
aria-controls
aria-current
aria-describedby
aria-description
aria-details
aria-flowto
aria-hidden
aria-keyshortcuts
aria-label
aria-labelledby
aria-live
aria-owns
aria-relevant
aria-roledescription
```

---

### BaseComponentProps
`src/design/shared/base.props.ts`

67/67 components

```
action
class
mDistance
motion
style
testId
v
```

---

## Nearly Universal Props

`bg` prop.

### Implementations

```
62 components → BaseComponentProps
5 components → DataProps
```

DataProps components

```
feed
list
metric
stat
table
```

---

`disabled` prop.

### Implementations

```
64 components → BaseComponentProps
```

### Overrides

```
CardProps
  • surfaces/card

TileProps
  • surfaces/tile

TriggerProps
  • triggers/skip-link
```

---

`id` prop.

### Implementations

```
60 components → BaseComponentProps
```

### Overrides

```
FieldProps
  • forms/field

AlertDialogProps
  • overlays/alert-dialog

DrawerProps
  • overlays/drawer

DropdownMenuProps
  • overlays/dropdown-menu

ModalProps
  • overlays/modal

PopoverProps
  • overlays/popover

SheetProps
  • overlays/sheet
```

---

`loading` prop.

### Implementations

```
66 components → BaseComponentProps
```

### Overrides

```
TriggerProps
  • triggers/skip-link
```

---

`target` prop.

### Implementations

```
64 components → BaseComponentProps
```

### Overrides

```
CardProps
  • surfaces/card

LinkProps
  • triggers/link

SkipLinkProps
  • triggers/skip-link
```

---

## Shared Props

These props appear in multiple interface families.

`size` prop.

### Implementations

```
9 → FeedbackProps
9 → TypographyProps
6 → NavProps
5 → DataProps
4 → OverlaysProps
1 → AudioProps
1 → AvatarProps
1 → FilePreviewProps
1 → IconProps
1 → DrawerProps
1 → TriggerProps
1 → LabelProps
```

---

`radius` prop.

### Implementations

```
7 → FeedbackProps
7 → SurfaceProps
6 → NavProps
4 → OverlaysProps
2 → SkeletonProps
1 → AudioProps
1 → AvatarProps
1 → FilePreviewProps
1 → ImageProps
1 → AlertProps
1 → BannerProps
1 → ToastProps
1 → BoxProps
1 → PopoverProps
1 → TooltipProps
1 → TriggerProps
```

---

`variant` prop.

### Implementations

```
7 → FeedbackProps
7 → SurfaceProps
6 → NavProps
5 → DataProps
4 → OverlaysProps
2 → SkeletonProps
1 → AudioProps
1 → AlertProps
1 → BannerProps
1 → ToastProps
1 → SeparatorProps
1 → TriggerProps
```

---

`color` prop.

### Implementations

```
10 → TypographyProps
6 → FeedbackProps
6 → NavProps
5 → DataProps
1 → AudioProps
1 → IconProps
1 → AlertProps
1 → BannerProps
1 → SpinnerProps
1 → ToastProps
1 → TriggerProps
```

---

`as` prop.

### Implementations

```
1 → ChipProps
1 → BoxProps
1 → ContainerProps
1 → FlexProps
1 → GridProps
1 → InlineProps
1 → ScreenProps
1 → SpacerProps
1 → StackProps
1 → CardProps
1 → FrameProps
1 → PanelProps
1 → PaperProps
1 → SectionProps
1 → TileProps
1 → WellProps
1 → CaptionProps
1 → CodeProps
1 → ProseProps
1 → TextProps
1 → VisuallyHiddenProps
```

---

`align` prop.

### Implementations

```
10 → TypographyProps
9 → LayoutProps
1 → StackProps
```

---

`icon` prop.

### Implementations

```
15 → IconProps
1 → SpinnerProps
```

---

`gap` prop.

### Implementations

```
9 → LayoutProps
1 → StackProps
1 → PaperProps
```

---

`wrap` prop.

### Implementations

```
10 → TypographyProps
1 → FlexProps
```

---

`clamp` prop.

### Implementations

```
10 → TypographyProps
```

---

`decoration` prop.

### Implementations

```
10 → TypographyProps
```

---

`fam` prop.

### Implementations

```
10 → TypographyProps
```

---

`fontStyle` prop.

### Implementations

```
10 → TypographyProps
```

---

`justify` prop.

### Implementations

```
9 → LayoutProps
1 → StackProps
```

---

`leading` prop.

### Implementations

```
10 → TypographyProps
```

---

`placement` prop.

### Implementations

```
7 → FeedbackProps
1 → DrawerProps
1 → PopoverProps
1 → TooltipProps
```

---

`tracking` prop.

### Implementations

```
10 → TypographyProps
```

---

`transform` prop.

### Implementations

```
10 → TypographyProps
```

---

`truncate` prop.

### Implementations

```
10 → TypographyProps
```

---

`weight` prop.

### Implementations

```
8 → TypographyProps
1 → HeadingProps
1 → LabelProps
```

---

`orientation` prop.

### Implementations

```
5 → NavProps
1 → FeedProps
1 → ListProps
1 → SeparatorProps
1 → StepperProps
```

---

`glass` prop.

### Implementations

```
7 → SurfaceProps
1 → NavbarProps
```

---

`blur` prop.

### Implementations

```
7 → SurfaceProps
```

---

`interactive` prop.

### Implementations

```
5 → DataProps
1 → CardProps
1 → TileProps
```

---

`layer` prop.

### Implementations

```
7 → SurfaceProps
```

---

`outlined` prop.

### Implementations

```
7 → SurfaceProps
```

---

`padding` prop.

### Implementations

```
7 → SurfaceProps
```

---

`pulse` prop.

### Implementations

```
7 → FeedbackProps
```

---

`selectable` prop.

### Implementations

```
5 → DataProps
1 → CardProps
1 → TileProps
```

---

`shadow` prop.

### Implementations

```
7 → SurfaceProps
```

---

`label` prop.

### Implementations

```
1 → AudioProps
1 → MetricProps
1 → StatProps
1 → SpinnerProps
1 → SkipLinkProps
1 → CaptionProps
```

---

`title` prop.

### Implementations

```
1 → EmptyStateProps
1 → ToastProps
1 → AlertDialogProps
1 → DrawerProps
1 → ModalProps
1 → SheetProps
```

---

`activeId` prop.

### Implementations

```
5 → NavProps
```

---

`bordered` prop.

### Implementations

```
5 → DataProps
```

---

`caption` prop.

### Implementations

```
5 → DataProps
```

---

`empty` prop.

### Implementations

```
5 → DataProps
```

---

`highlight` prop.

### Implementations

```
5 → DataProps
```

---

`scrollable` prop.

### Implementations

```
5 → DataProps
```

---

`striped` prop.

### Implementations

```
5 → DataProps
```

---

`height` prop.

### Implementations

```
2 → SkeletonProps
1 → ImageProps
1 → ScreenProps
```

---

`href` prop.

### Implementations

```
1 → CardProps
1 → TileProps
1 → LinkProps
1 → TriggerProps
```

---

`items` prop.

### Implementations

```
1 → BreadcrumbsProps
1 → MenuProps
1 → StepperProps
1 → TabsProps
```

---

`ratio` prop.

### Implementations

```
2 → SkeletonProps
1 → ImageProps
1 → FrameProps
```

---

`src` prop.

### Implementations

```
1 → AudioProps
1 → AvatarProps
1 → FilePreviewProps
1 → ImageProps
```

---

`closeOnEsc` prop.

### Implementations

```
1 → AlertDialogProps
1 → DrawerProps
1 → ModalProps
```

---

`data` prop.

### Implementations

```
1 → FeedProps
1 → ListProps
1 → TableProps
```

---

`description` prop.

### Implementations

```
1 → MetricProps
1 → EmptyStateProps
1 → AlertDialogProps
```

---

`direction` prop.

### Implementations

```
1 → SpinnerProps
1 → CenterProps
1 → FlexProps
```

---

`dismissible` prop.

### Implementations

```
1 → AlertProps
1 → BannerProps
1 → ToastProps
```

---

`dismissLabel` prop.

### Implementations

```
1 → AlertProps
1 → BannerProps
1 → ToastProps
```

---

`layout` prop.

### Implementations

```
1 → AudioProps
1 → FilePreviewProps
1 → TableProps
```

---

`rel` prop.

### Implementations

```
1 → CardProps
1 → LinkProps
1 → TriggerProps
```

---

`sticky` prop.

### Implementations

```
1 → BannerProps
1 → HeaderProps
1 → NavbarProps
```

---

`type` prop.

### Implementations

```
1 → ProgressProps
1 → TriggerProps
1 → QuoteProps
```

---

`value` prop.

### Implementations

```
1 → MetricProps
1 → StatProps
1 → ProgressProps
```

---

`width` prop.

### Implementations

```
2 → SkeletonProps
1 → ImageProps
```

---

`alt` prop.

### Implementations

```
1 → AvatarProps
1 → ImageProps
```

---

`animated` prop.

### Implementations

```
2 → SkeletonProps
```

---

`closeOnBackdrop` prop.

### Implementations

```
1 → DrawerProps
1 → ModalProps
```

---

`columns` prop.

### Implementations

```
1 → TableProps
1 → GridProps
```

---

`fit` prop.

### Implementations

```
1 → ImageProps
1 → GridProps
```

---

`iconOnly` prop.

### Implementations

```
1 → BadgeProps
1 → TagProps
```

---

`lines` prop.

### Implementations

```
2 → SkeletonProps
```

---

`max` prop.

### Implementations

```
1 → BadgeProps
1 → ProgressProps
```

---

`maxWidth` prop.

### Implementations

```
1 → ContainerProps
1 → NavbarProps
```

---

`name` prop.

### Implementations

```
1 → FilePreviewProps
1 → IconProps
```

---

`required` prop.

### Implementations

```
1 → FieldProps
1 → LabelProps
```

---

`selected` prop.

### Implementations

```
1 → CardProps
1 → TileProps
```

---

## Component-only Props

These props exist on only a single component.

### AudioProps

```
autoPlay
intercomMode
loop
muted
playbackRate
preload
preservePitch
redactSegments
volume
```

---

### AvatarProps

```
initials
status
```

---

### BadgeProps

```
count
dot
```

---

### BreadcrumbsProps

```
separatorIcon
separatorText
```

---

### CaptionProps

```
credit
overlay
rule
```

---

### CodeProps

```
block
```

---

### FeedProps

```
grouped
```

---

### FieldProps

```
invalid
```

---

### FilePreviewProps

```
fileSize
onRemove
removable
```

---

### FrameProps

```
clip
```

---

### HeadingProps

```
level
```

---

### ImageProps

```
imgLoading
```

---

### IndentProps

```
code
prose
ui
```

---

### LabelProps

```
for
```

---

### LinkProps

```
external
underline
```

---

### ListProps

```
ordered
```

---

### MenuProps

```
indentChildren
```

---

### MetricProps

```
trend
trendDirection
```

---

### PaginationProps

```
baseUrl
currentPage
getPageUrl
showControls
siblingCount
totalPages
```

---

### PaperProps

```
fullWidth
stack
```

---

### ProgressProps

```
indeterminate
showValue
```

---

### QuoteProps

```
cite
```

---

### ScreenProps

```
centered
overflow
```

---

### SeparatorProps

```
strength
```

---

### SheetProps

```
side
```

---

### SpinnerProps

```
speed
```

---

### StepperProps

```
currentStepIndex
```

---

### TableProps

```
sort
sortable
stickyHeader
```

---

### TabsProps

```
fitted
```

---

### TooltipProps

```
content
```

---

## Interface Statistics

| Interface | Components | Props |
|-----------|-----------:|------:|
| AriaProps | 67 | 15 |
| SpacingProps | 67 | 14 |
| BaseComponentProps | 67 | 12 |
| IconProps | 16 | 4 |
| TypographyProps | 10 | 13 |
| FeedbackProps | 9 | 6 |
| LayoutProps | 9 | 3 |
| SurfaceProps | 7 | 8 |
| NavProps | 6 | 6 |
| DataProps | 5 | 12 |
| OverlaysProps | 4 | 3 |
| SkeletonProps | 2 | 7 |
| AudioProps | 1 | 16 |
| TriggerProps | 1 | 9 |
| CardProps | 1 | 8 |
| FilePreviewProps | 1 | 8 |
| ImageProps | 1 | 8 |
| AvatarProps | 1 | 6 |
| BannerProps | 1 | 6 |
| DrawerProps | 1 | 6 |
| PaginationProps | 1 | 6 |
| TableProps | 1 | 6 |
| TileProps | 1 | 6 |
| ToastProps | 1 | 6 |
| AlertProps | 1 | 5 |
| CaptionProps | 1 | 5 |
| LinkProps | 1 | 5 |
| MetricProps | 1 | 5 |
| ProgressProps | 1 | 5 |
| SpinnerProps | 1 | 5 |
| AlertDialogProps | 1 | 4 |
| BadgeProps | 1 | 4 |
| LabelProps | 1 | 4 |
| ModalProps | 1 | 4 |
| PaperProps | 1 | 4 |
| ScreenProps | 1 | 4 |
| StackProps | 1 | 4 |
| BreadcrumbsProps | 1 | 3 |
| FeedProps | 1 | 3 |
| FieldProps | 1 | 3 |
| FlexProps | 1 | 3 |
| FrameProps | 1 | 3 |
| GridProps | 1 | 3 |
| IndentProps | 1 | 3 |
| ListProps | 1 | 3 |
| NavbarProps | 1 | 3 |
| PopoverProps | 1 | 3 |
| SeparatorProps | 1 | 3 |
| SheetProps | 1 | 3 |
| StepperProps | 1 | 3 |
| TooltipProps | 1 | 3 |
| BoxProps | 1 | 2 |
| CodeProps | 1 | 2 |
| ContainerProps | 1 | 2 |
| EmptyStateProps | 1 | 2 |
| HeadingProps | 1 | 2 |
| MenuProps | 1 | 2 |
| QuoteProps | 1 | 2 |
| SkipLinkProps | 1 | 2 |
| StatProps | 1 | 2 |
| TabsProps | 1 | 2 |
| CenterProps | 1 | 1 |
| ChipProps | 1 | 1 |
| DropdownMenuProps | 1 | 1 |
| HeaderProps | 1 | 1 |
| InlineProps | 1 | 1 |
| PanelProps | 1 | 1 |
| ProseProps | 1 | 1 |
| SectionProps | 1 | 1 |
| SpacerProps | 1 | 1 |
| TagProps | 1 | 1 |
| TextProps | 1 | 1 |
| VisuallyHiddenProps | 1 | 1 |
| WellProps | 1 | 1 |

---

## Interesting Architecture Notes

✓ All components inherit accessibility through `AriaProps`.

✓ All components inherit styling through `BaseComponentProps`.

✓ `disabled` has three independent implementations.

✓ `bg` is intentionally duplicated between `BaseComponentProps` and `DataProps`.

✓ `size` is implemented by multiple interface families and does not have a single semantic definition.

✓ 59 props are unique to a single component.
